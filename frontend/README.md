# Frontend UI

Simple React + Vite interface to showcase and test all backend API endpoints.

## Run

```bash
cd frontend
npm install
npm run dev
```

The app defaults to `http://localhost:8000`.

To override API URL, create `.env` in this folder:

```dotenv
VITE_API_BASE_URL=http://localhost:8000
```
